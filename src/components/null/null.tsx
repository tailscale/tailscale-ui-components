import React from "react"

/**
 * Component to display a null value (which right now is just an mdash).
 */
export function NullValue() {
  return <span className="text-text-disabled">&mdash;</span>
}
